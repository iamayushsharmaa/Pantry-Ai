import 'package:flutter/material.dart';

class DishesDetailScreen extends StatelessWidget {
  const DishesDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
